import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asteroid.simulator',
  appName: 'Asteroid Simulator',
  webDir: 'out',              
  bundledWebRuntime: false
};

export default config;